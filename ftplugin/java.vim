" Java specific settings

set textwidth=99    " limit lines to 99 characters
set tabstop=2		" set tabs to have 2 spaces
set shiftwidth=2	" when using >> or << commands, shift lines by 2 spaces
set softtabstop=2	" set tabs to have 2 spaces in insert mode
set expandtab		" expand tabs into spaces
set autoindent		" indent when moving to the next line
